/// Shared constant for the SharedPreferences key that holds the active
/// family ID. Extracted here to avoid circular imports when both
/// sync_lifecycle_controller and feature repositories need the same key.
const String kFamilyIdPrefsKey = 'family.id';
